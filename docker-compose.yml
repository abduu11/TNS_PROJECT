version: "3.9"

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    volumes:
      - ./database:/app/database
      - ./segments:/app/segments
      - ./uploads:/app/uploads
    restart: unless-stopped
